# Additional AI service dependencies
# Add these to your requirements.txt

# For Groq support (fast inference)
groq>=0.4.1

# For enhanced .env support
python-dotenv>=1.0.0

# Current OpenAI (already in requirements.txt but ensure latest)
openai>=1.0.0
